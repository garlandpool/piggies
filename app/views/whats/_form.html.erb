<%= form_for(@what) do |f| %>
  <% if @what.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@what.errors.count, "error") %> prohibited this what from being saved:</h2>

      <ul>
      <% @what.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="panel-body">
    <div class="field">
      <%= f.label :font_awesome %><br>
      <%= f.text_field :font_awesome, { class: 'form-control' } %>
    </div>
    <div class="field">
      <%= f.label :title %><br>
      <%= f.text_field :title, { class: 'form-control' } %>
    </div>
    <div class="field">
      <%= f.label :body %><br>
      <%= f.text_area :body, { class: 'form-control' } %>
    </div>
    <br/>

    <div class="actions">
      <%= f.submit "Update", class: "btn-sm btn-primary" %>
    </div>
  </div>
<% end %>


